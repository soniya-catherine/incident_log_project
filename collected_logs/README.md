# collected_logs

Place exported Windows and Ubuntu log files in this folder before running the normalization script.

This folder is intentionally included so users do not need to create it manually.